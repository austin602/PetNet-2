<h2>Users</h2>

<a class="btn btn-primary"href="/admin/create">Create User</a>


{{#each data.usersList}}
    <div>
        <div>
            <strong>Name </strong> <a href="/admin/{{id}}">{{username}}</a>
        </div>
        <div>
            <strong>E-Mail</strong> {{email}}
        </div>
        <div>
            <strong>Admin</strong> {{admin}}
        </div>
    </div>
{{/each}}
